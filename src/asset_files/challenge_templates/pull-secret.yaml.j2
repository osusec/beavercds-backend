apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: "rcds-{{ slug }}-pull"
  namespace: "{{ namespace }}"
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "{{ registry_domain }}": {
          "auth": "{{ creds_b64 }}"
        }
      }
    }
